name: Build

on:
  workflow_dispatch:
    inputs:
      environment:
        type: choice
        options:
          - 'prod'
          - 'staging'
        default: 'prod'
      
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Build to ${{ github.event.inputs.environment}}
      uses: appleboy/ssh-action@master
      with:
        username: ${{ secrets.SSH_USER }}
        key: ${{ secrets.SSH_KEY }}
        host: ${{ secrets.SSH_HOST }}
        script: sh /opt/ps2gg/ps2.gg/build.sh ${{ github.event.inputs.environment}}