import { CommandConfig } from '@ps2gg/discord/command'

export const SetupEarlyAccess: CommandConfig = {
  name: 'early-access',
  description: 'Makes early access channels available to subscribers',
}
